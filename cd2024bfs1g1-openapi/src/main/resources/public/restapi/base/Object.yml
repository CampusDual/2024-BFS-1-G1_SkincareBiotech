components:
  schemas:
    Object:
      type: object

  requestBodies:
    Object:
      required: true
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Object'
